import React from 'react';
import CategoryRectangle from '../CategoryRectangle/CategoryRectangle'; // Make sure to import the CategoryRectangle component
import motorbikeImage from '../../assets/images/motocat.jpg'; // Update the path to your motorbike image

function MotosPage() {
  return (
    <div>
      <CategoryRectangle image={motorbikeImage} />
      {}
    </div>
  );
}

export default MotosPage;
